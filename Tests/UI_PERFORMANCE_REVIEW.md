# 相册式界面、缩略图和播放外围检查

日期：2026-09-09。对象：根目录 Gallery115.xcodeproj 实际引用的 Gallery115/；未修改历史副本或未跟踪的 115/。

## 已实现

| 范围 | 变化 | 使用目的 |
| --- | --- | --- |
| 资料库、收藏 | 默认 2 pt 间距、方形封面、细圆角；可切回显示标题的宽屏封面模式 | 内容更集中，浏览接近相册 |
| 交互动画 | 0.18 秒按压回弹、加载淡入；沿用 iOS 18+ 系统缩放转场，iOS 17 用系统默认呈现 | 操作有反馈，打开与返回有连续性 |
| 辅助功能 | 减少动态效果时关闭缩放/弹性动画；紧凑封面保留文件名无障碍标签 | 尊重系统偏好 |
| 滚动 | 去掉单卡片阴影和收藏标记的材质叠层；启动动画完成后不再用 Logo 的滚动位置更新整个根界面 | 减少重复绘制与大范围刷新 |
| 缩略图排队 | 可见请求优先于预加载；一次只进行一项预加载；图片阶段最多 3 项，抽帧阶段独立最多 2 项 | 慢视频不阻塞现成图片 |
| 海报 | 复用同目录文件索引；公共海报数据使用最多 24 MiB 的内存缓存 | 减少目录扫描与重复下载 |
| OpenList | 从已有刷新响应读取 thumb，最多 10,000 条返回项；失败封面在刷新完成后重新尝试 | 利用服务端已有缩略图，减少本地抽帧 |
| 分页/搜索 | 无匹配项时继续分页；新查询/刷新拒绝旧响应；搜索输入不再重复排序播放队列 | 防止假空目录和无用计算 |
| 播放 | 取消准备后不再发布流地址或启动播放；115 取消请求不再继续退避重试或预占未来请求时段 | 减少退出后继续请求以及起播竞争 |
| 照片 | 真正请求并缓存图片预览；收藏照片打开照片预览而非视频播放器 | 修复原来的占位和类型分发问题 |

按压动画和图片淡入作用于单个元素，没有给整屏数据加载加统一动画。文件夹列数仍保留菜单退出后更新策略。媒体双指缩放期间仅改变显示比例，松手后调整列数，保留卡片身份。

## 不变的部分与限制

- AVPlayer/VLC 播放实现、清晰度偏好、播放队列业务、收藏存储、Token 存储和 WebDAV 播放地址生成没有重构。PlayerModel 仅在 prepareAndPlay 中增加取消保护。
- 新照片界面是最长边 960 像素的缓存预览，不是原图编辑器或完整的系统相册替代品。
- OpenList 缩略图补充支持首次读取目录，使用 refresh=false 的只读列表并合并同目录请求；登录失败或服务端本身没有 thumb 时仍会回退。没有承诺所有挂载都能返回现成封面。
- 无现成封面的 WebDAV 视频仍需要远程抽帧。网络速度、封装、编码和服务器 Range 支持会影响耗时；系统不支持的格式仍可能无封面。更多并发不一定更快。
- 115 直连已有代码入口，但 Info.plist 的 Cineva115AuthorizationURL 仍为空。用户确认尚无开放平台应用/授权服务，本轮按要求不部署、不改登录配置。

## 验证结果

- 执行 `python -u Tests/preflight.py`：0 个新增预检失败。
- 全部 App Swift 文件纳入语法预检；AppState 的 17 条与 PlayerModel 的 4 条 tree-sitter 诊断与 Git 基线完全一致，标为 BASELINE。该解析器不能替代 Swift 类型检查。
- 执行 `git diff --check`：通过。
- 新增 4 个 XCTest：可见队列优先、慢抽帧与图片队列隔离、照片缓存跨服务实例复用、OpenList 缩略图字段解析与无效地址处理。
- 30 个 iOS XCTest **尚未运行**；当前 Windows 没有 Xcode、iPhone 模拟器或真机测试通道。未测 FPS、掉帧数、首屏/起播秒数。

## iPhone 验收路径

1. 在 Mac 用根目录工程 Clean Build；覆盖安装，保留缓存和数据。
2. 同一目录选择 100～300 项：分别测冷缓存和热缓存首屏，快速连续上下滚动；确认离屏请求退让，现成图片不被慢视频卡住。
3. 向下滚动后调整 2/3/4 列，切换方形/宽屏，切换筛选和排序；确认位置稳定、无闪白和崩溃。测试当前页没有目标类型但后续页有目标类型的目录。
4. 缩略图正在加载时打开视频，立即退出，再打开另一视频；确认没有旧视频声轨、迟到画面或永久等待。
5. 分别播放 MP4 和 VLC 接管的 MKV：续播、拖动进度、字幕、倍速、画中画、返回；比较同一视频相同网络条件下的起播耗时。
6. 开启系统“减少动态效果”，确认按压不缩放、呈现不过度运动；关闭后检查淡入和缩放衔接。用 Instruments 的 Animation Hitches/Time Profiler 测实际帧表现。
7. 进入照片筛选，查看预览和收藏，再从收藏打开照片；后台锁定后内容应被遮挡。
8. 运行 Tests/README.md 中的 iOS XCTest 命令。

## 依据

- Apple 系统缩放转场：https://developer.apple.com/documentation/swiftui/zoomnavigationtransition
- Apple 惰性网格：https://developer.apple.com/documentation/swiftui/lazyvgrid
- OpenList fs/list 的 content、name、thumb 字段：https://github.com/OpenListTeam/OpenList/blob/main/server/handles/fsread.go
- 115 开放平台：https://open.115.com/
- Infuse 官网列出的云盘支持：https://firecore.com/infuse

## 本轮：媒体缩放与持续转圈修复

- 视频、图片支持双指缩放至 1/2/3/4/6 列，带松手重排动画及减少动态效果适配；资料库文件夹恢复 16:9 宽卡片、名称和说明。
- 图片发现/下载阶段 18 秒、视频源解析和抽帧阶段 15 秒硬截止，取消后不等待不响应取消的底层任务。卡片包含排队和自动重试的总等待上限 45 秒，到期显示占位，可长按“重试缩略图”。这不意味着 45 秒内必定生成图片。
- 预加载不主动远程抽帧，也不把预加载未找到封面写成失败缓存；可见媒体使用两个独立抽帧通道。已经取消的播放准备不能再关闭缩略图通道。
- 首次浏览同目录合并一次 OpenList 只读封面发现，独立会话每个请求资源超时 8 秒，失败冷却 30 秒。保留原 WebDAV 播放地址与请求头逻辑。
- 新增硬截止、不响应取消的加载器、缩放边界回归用例，现有抽帧隔离测试适配两通道。
- Windows 仅可运行源码预检及差异检查；33 个 XCTest 尚未执行，Xcode 类型检查、双指动画与真实 OpenList 大目录性能仍需 Mac/iPhone 验证。

## 后续修复：缩放误触与卡顿

- 缩放改为高优先级手势，卡片播放动作共享一个不触发视图刷新的点击门控，手势更新及结束后 0.5 秒拒绝打开视频/照片，取消手势复位时也延长保护。
- 将连续缩放状态移入独立 ViewModifier，避免每帧重新执行媒体网格的单元构建；跟随起始触点缩放，松手只提交一次列数变化，取消整个懒网格的几何插值动画。仍保留手指驱动的连续缩放。
- 照片直接加载其图片源，不再等待 OpenList 登录与列表发现。视频的海报与服务端封面并行查找，首个成功结果立即返回并取消另一分支；某一分支返回空不会抢先宣告失败。
- 新增两组候选封面竞速回归，验证慢分支不阻塞现成图片、空结果不吞掉后续成功，以及全部失败返回空。
- 源码预检与 git diff --check 已通过。35 个 iOS XCTest 仅已编写，Windows 无法执行 Xcode/真机验证；没有可报告的帧率、首图延迟改善数值。

## 补充：有封面却被显示为缺失

移除把排队时间包含在内的卡片 45 秒总截止，避免繁忙目录尚未开始下载的卡片被误判。保留实际图片阶段 18 秒、抽帧阶段 15 秒的截止与取消释放。可见卡片在临时失败后按 6/15/30/60 秒退避自动重试，离屏取消；失败文案改为“加载未完成，可长按重试”，不再把一次网络失败表述为没有缩略图。重试等待期间停止转圈。以上取代上一节关于卡片 45 秒总等待的描述。

## 相册缩放衔接、播放页与设置页（本轮）

- 参考 Apple 的相册双指缩放使用说明，以及 SwiftUI 性能文档关于减少布局计算和保留视图身份的建议。没有苹果相册私有动画代码或真机对比证据，不宣称完全复刻。
- 松手不再把 GestureState 的比例立即重置。切换列数时按新旧列数比补偿显示比例，下一次布局安装后用 0.32 秒弹簧收拢，仅动画外层变换。手势中心保留、正常范围跟随手指，边缘使用渐进阻力；中断、离屏取消和减少动态效果均有处理。
- 保留高优先级双指识别和松手点击保护。新编写 2 个缩放数学边界测试；它们不证明视觉帧率或滚动位置完全稳定。
- 播放页增加片名、缓冲/播放/暂停状态和常显时间，扩大主要按钮至 44pt；按压效果支持减少动态效果。未改播放引擎与请求协议。
- 设置页按媒体与外观、播放体验、管理分组，统一彩色图标并展示当前媒体源配置；修复封面列数实际只改变文件夹的误导，媒体列数与相册模式接入资料库相同持久化键。
- 按用户要求，未取到封面时只保留占位，不显示缺失/失败文案。可见且前台时持续退避重试，间隔上限 60 秒；进入后台取消，回前台继续。播放器实际错误提示保留。
- 远程抽帧预算随尝试从 15 秒增加到 30、60 秒；同步调整原有抽帧内部 20 秒取消点为 60 秒，避免后续延长预算无效。不会按视频时长直接断言是否能取封面。
- Windows 源码预检与差异检查通过；37 个 XCTest 已编写但未执行。Xcode 类型检查、真机动画、动态字体横竖屏和真实网盘响应仍待验证。

参考：https://support.apple.com/en-euro/guide/iphone/iph7d24753a5/ios
参考：https://developer.apple.com/documentation/Xcode/understanding-and-improving-swiftui-performance

## 灵动岛遮挡、返回大目录白屏与滚动（本轮）

- 竖屏播放器顶部间距改为 max(实际安全区顶部, 59pt) + 32pt，覆盖全屏容器返回零安全区的情况；横屏仍按左右安全区布局。需真机确认机型实际遮挡情况。
- 播放设置面板按可用安全区域限定宽高，选项改为至少 44pt 高，长标题最多三行并自动增高，增加关闭按钮标签及选中语义。内容使用懒纵向布局，保留滚动指示。
- 设置首页恢复 5a49f0a 版本的单组列表、原始图标和说明；保留修正后的独立媒体列数选项。
- 查到返回资料库时重复首屏读取会将已经加载的多页裁成 56 项的路径。现在同媒体源、文件夹、排序已成功加载时复用现有内容；改变来源/排序才重建，保留正常分页和手动刷新。
- 将滚动定位状态移到独立容器，避免每跨过一行就重新计算整个 FolderView；筛选/刷新移除目标项时清理定位。去掉包裹两个懒网格的外层 LazyVStack，避免整个网格成为一个懒加载单元。
- 视频封面统一按最长边 640px 解码，抽帧上限由 960x540 降为 640x360，旧磁盘缓存读取也缩采样；照片仍为 960px。视频位图像素上限约为此前 44%，这不是实测帧率提升百分比。单列放大视频封面的细节相应受 640px 限制。
- 新增大图旧缓存缩采样 XCTest。源码预检、差异检查通过；38 个 XCTest 尚未运行，Windows 无 Xcode/iPhone，白屏是否完全消除和滚动帧率仍需设备验证。

## 根目录变化后的收藏、批量选择与首帧缓存（本轮）

- 原收藏仅按 WebDAV 路径判断。刷新列表先核对旧收藏：路径相同更新快照；路径变化但文件名、正文件大小、非空 ETag 与修改时间均相同且当前列表唯一匹配时迁移到当前路径。迁移后去重并沿用原 favorites.v1 存储格式；未匹配、缺版本或有歧义时保留旧记录。ETag 不被当作内容 SHA-1；历史收藏没有原服务器来源信息，此策略不能证明文件字节完全一致，也不能覆盖服务端同时重写版本信息的情况。
- 资料库筛选菜单增加“选择视频”，提供“全选已加载”、收藏和取消收藏；收藏页右上角选择模式支持批量取消。选择模式不会播放，目录导航停用，照片不参与视频选择。一次批量更新只发布一次集合并写一次收藏，不涉及网盘文件删除。
- 卡片首帧直接使用线程安全的内存封面快照；主线程只查询缓存，不访问钥匙串/磁盘或解码。首次目录渲染前由缓存 actor 预读前 24 项本地封面，不发网盘请求。缓存被系统释放或深处项目尚未预读时仍需要异步磁盘读取，不承诺冷缓存绝无占位帧。
- 资料库缩放限制整个网格的显示比例不低于视口宽度，水平中心固定；切换列数后重新对齐当前可见项，并裁剪滚动视口，防止缩小整个长列表露出边缘空白。仍待真机确认滚动/缩放过程中位置衔接。
- 新增实际 LibraryStore 回归覆盖旧格式加载与迁移持久化、批量幂等与只取消选中项、歧义/缺版本保护、原路径优先与去重；新增本地缓存预读和同步首帧查询回归。测试包现在编译实际 LibraryStore 与 PlaybackEntry；没有替换播放器业务。
- Windows 源码预检及差异检查通过。43 个 XCTest 已编写但未执行，Xcode 类型检查、网盘路径匹配与真机交互尚待验证。

ETag 语义参考：https://github.com/OpenListTeam/OpenList/blob/main/server/webdav/webdav.go

## 回归修复：双指与滚动冲突、停住与播放竞争（本轮）

- 将资料库和收藏网格的 SwiftUI MagnifyGesture 换为附着于实际 UIScrollView 的原生 UIPinchGestureRecognizer。滚动 pan 限制为单指；双指 pinch 开始时立即取消并恢复 pan，使前一根手指的拖动不能继续抢占。两个手指只在媒体网格区域触发，移除桥接视图时恢复原 pan 设置，保留点击保护。列数切换死区从 12% 调为 6%。
- 去掉 ScrollViewReader.scrollTo 与 scrollPosition 并用、列数变化后主动滚动的补偿路径，仅保留系统定位绑定和无效定位清理。这是代码中可疑的重复布局来源，尚无真机堆栈证明它是全部卡死现象的根因。
- 播放进入时显式取消 WebDAV/OpenList 独立封面发现任务，避免共享 Task 在卡片请求取消后继续请求网络。增加发现任务代次保护，旧任务结束不能清掉新任务槽位。
- 已退出播放器的 owner 不能被迟到的 suspend 重新登记；播放器再次准备时使用新的 owner。磁盘缓存预读每项之间让出 actor，播放开始后停止预读，避免解码队列拖住播放优先权获取。
- 没有降低原视频画质、修改 AVPlayer/VLC 播放地址或改变核心解码/缓冲策略。网络速度与码率仍需真实网盘测试，不能从静态检查推出首帧或持续播放的提升幅度。
- 新增退出后迟到 acquire 的队列回归。Windows 源码预检与差异检查通过；44 个 XCTest 已编写但未执行。原生手势、取消/恢复滚动、卡死堆栈和真实网盘播放必须在 Xcode/iPhone 上验证。

运行环境复核：本轮调用 XcodeBuildMCP 的模拟器列表返回 `spawn xcrun ENOENT`，没有可用模拟器，因此未执行 iOS 构建或手势测试。


## 2026-09-10：按开源网格过渡实现重新设计缩放

本节替代上述整张 LazyVGrid 的 transform/scrollPosition 缩放方案。

- 资料库和收藏页改用同一个 UICollectionView；SwiftUI 只提供卡片内容，滚动及布局由 UIKit 持有。文件夹保持原 FolderCard 外观及独立列数，媒体列数沿用 1/2/3/4/6。搜索、筛选、分页、下拉刷新和批量选择仍接入原业务。
- 捏合驱动 UICollectionViewTransitionLayout 的连续进度，布局仅计算查询区域内的行。身份按文件 ID 去重；过渡期间延后数据快照更新，结束后应用最新结果。不存在每帧修改 SwiftUI 列数或整张长列表缩放。
- 开源对照发现仅在手势 changed 中修改 offset 不够：原生结束/取消动画也必须同步定位，并在最终布局安装后恢复该定位。新增 PhotoGridTransitionLayout，在进度 setter 中插值焦点位置与内容高度、限制滚动边界；完成回调恢复定位并清除临时焦点。
- 手势反向越过原始比例时取消当前方向的过渡；松手结合短时、限幅速度预测决定完成/回退。一个手势完成一档后可继续下一档；原生收尾期间短暂忽略 changed，实际跨档手感仍需设备检查。
- 未引入旧版 Objective-C 库或 React Native 依赖，也未直接复制外部源码。TLLayoutTransitioning 较早，源码每帧枚举全部项目不适合直接搬入大目录；react-native-zoom-grid 是另一技术栈的交互参考，不是本项目运行时组件。此实现没有复刻其多层淡入淡出，也不能声称与苹果相册完全一致。
- 源码预检、差异检查用于静态验证。新增双向进度、反向与非法值、速度投影测试，47 个 XCTest 已准备但未执行。当前 Windows 没有 xcrun/Xcode，尚未通过 iOS 类型检查、真机动画、万项目录性能或手势冲突实测。

参考（直接阅读仓库实现和 Apple API 文档）：
- https://github.com/SwiftKickMobile/TLLayoutTransitioning
- https://github.com/SwiftKickMobile/TLLayoutTransitioning/blob/master/TLLayoutTransitioning/TLTransitionLayout.m
- https://github.com/wassgha/react-native-zoom-grid/blob/main/src/ZoomGrid.tsx
- https://developer.apple.com/documentation/uikit/uicollectionview/startinteractivetransition(to:completion:)

设备验收：在目录中部、最后一屏和不足一屏时双向捏合；缓慢反向、快速松手、跨多档；确认缩放后首个点击可播放；缩放时触发分页/刷新或返回；收藏批量取消及文件夹导航；大字体和横竖屏切换。必须观察内容边界、焦点跳动、转场停止及主线程耗时后才能确认手感修复。


## 2026-09-10：识别失败与误触回归

用户仍反馈难以触发、误开视频，因此上一节的交互不能视为验收通过。

代码可确认：原点击保护仅在 pinch action 收到 began 后启用，识别失败的双指操作没有保护；只允许与 pan 同时识别，并未阻止 UIHostingConfiguration 内部手势使父 pinch 失败。没有设备事件记录，不能断言这是所有触发失败的唯一原因。

修复：使用 UIPinchGestureRecognizer 子类，在 touchesBegan 观察到媒体区域内第二根手指时先阻止卡片动作并暂停 pan，仍由系统计算缩放比例；防止同一集合及后代视图的识别器提前使 pinch 失败，外部导航/系统手势仍遵循默认规则。保护持续到 reset，失败/取消/结束都会恢复滚动并短暂屏蔽残留点击；拆除视图也显式恢复。删除 began 里立即关闭又开启 pan 的操作；两指持有期间延迟快照重配。

参考 Apple 的自定义手势生命周期与冲突规则：
https://developer.apple.com/documentation/uikit/uigesturerecognizer/canbeprevented(by:)
https://developer.apple.com/documentation/uikit/uigesturerecognizer/touchesbegan(_:with:)

验证：Windows 源码预检、git diff --check。没有 Xcode/设备执行证据，47 个既有 XCTest 不覆盖 UIKit 手势竞争；必须测试双指无明显移动、先滑后加第二指、失败/取消后再次单指滚动、长按菜单、快速重复捏合、双指结束后视频点击，以及当前 IPA 的提交版本。未声称运行时已修复。
