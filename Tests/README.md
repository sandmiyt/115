# 封面持久缓存 / 起播竞争修复验证

## 实现与边界

- 封面存放在 `Library/Application Support/CinevaArtwork`，不设 TTL，不做容量自动淘汰，排除 iCloud 备份；内存缓存仍有上限。
- 磁盘键包含完整 WebDAV 端点、账号、挂载路径、文件路径、大小、修改时间；不包含临时封面 URL、密码或 WebDAV ETag。
- 旧 `Library/Caches/GeneratedThumbnails` 会迁入持久目录，再按旧键按需转换。旧版本未保存服务器身份，因此旧缓存只归属首次访问它的连接；无法反查的旧 ETag 文件不能可靠匹配，需要重建一次。
- 只有缓存缺失、损坏、文件信息变化，或用户手动清除/卸载，才重新获取封面。卸载重装不等于覆盖安装，无法保留 App 沙盒。
- 图片下载/海报发现最多 3 个任务，另有 1 个独立的视频抽帧任务；重复请求合并，最后一个消费者取消时停止任务，播放页持有优先权时停止远程封面工作。本地封面命中不受影响。
- 抽帧取消会停止 AVFoundation 请求；取靠近开头的帧，不再先加载完整时长以计算百分比时间点。
- 快速起播时，播放器旁路请求等待实际播放时间推进再启动。不修改 AVPlayer/VLC、115 登录、Token、WebDAV 302取流、播放记录、收藏和队列规则。
- WebDAV 元数据查询被取消时不写入“没有元数据”的负缓存，防止停止后台封面任务后使播放器海报/信息被误判为空。
- 日志类别 `PlaybackStartup` 分别记录交给内核、释放旁路请求的耗时；不记录账号、URL或签名。这不是像素级首帧测量。
- 不承诺固定起播秒数：OpenList/115的冷解析和CDN首字节延迟仍需同一网络下实测。

## iOS 运行测试（需 Mac + Xcode）

根目录 `Package.swift` 仅用于验证，直接编译真实缓存实现，网络/凭据边界使用测试替身。不会改变 App 的 CocoaPods 构建。

1. 在 Xcode 打开根目录 `Package.swift`，选择 `CinevaCacheValidation` scheme 和任意 iOS 17+ 模拟器。
2. 执行 Product → Test；应运行 30 个 XCTest。
3. 命令行也可先运行 `xcodebuild -list` 确认自动生成的 scheme，再执行：

```sh
xcodebuild -scheme CinevaCacheValidation \
  -destination 'platform=iOS Simulator,name=你的模拟器名称' \
  -resultBundlePath build/CacheRegression.xcresult \
  -test-timeouts-enabled YES -maximum-test-execution-time-allowance 60 test
```

测试覆盖：重建服务后磁盘命中且网络调用为0、数月旧文件不过期、签名/ETag变化、挂载/账号隔离、文件替换、旧缓存迁移、损坏修复、并发去重/限流、播放抢占/恢复、排队取消、清理期间迟到回调不复活文件、写盘失败、实际AVFoundation本地视频抽帧。

## 本机源码预检（不等同于 Xcode 编译/运行）

```powershell
python -m pip install --target .validation-tools tree-sitter==0.26.0 tree-sitter-swift==0.7.3
python Tests/preflight.py
git diff --check
```

## 真机验收

1. **覆盖安装**更新版本，保持 bundle ID 不变。不要先卸载。
2. 在资源库等待一组封面显示，查看“设置 → 缓存 → 本地封面”占用。
3. 强制退出 App，断开网络，再打开已经缓存的资源库：同一组封面应显示，不依赖115或签名链接。
4. 保持原文件不变，等待一天或刷新OpenList ETag，再次打开验证；若真实文件大小/修改时间变化，重建封面是预期行为。
5. 缩略图正在生成时打开视频，返回后确认未完成封面继续加载；重复快速打开、退出、切换视频。
6. 同一视频、相同续播位置、相同网络分别测冷启动和重复打开。记录修改前后实际等待时间，不用 UI 转圈时长推断网络根因。
7. 验证 AVPlayer 的 MP4 与 VLC 的 MKV、续播、暂停、字幕、队列、锁定、后台/恢复和旋转均未回归。

## 参考

- Apple 非可清理数据与备份排除：https://developer.apple.com/documentation/foundation/optimizing-your-app-s-data-for-icloud-backup
- Nuke 的内存/磁盘分层、请求合并与优先级：https://github.com/kean/Nuke
- SDWebImage 自定义缓存键：https://github.com/SDWebImage/SDWebImage/blob/master/SDWebImage/Core/SDWebImageManager.m

采用以上原则做局部实现，没有新增运行时第三方图片库。

## 2026-09-09 缩略图修复

- WebDAV 卡片只发现和下载海报，跳过 NFO；支持同名 jpg/png 和 -thumb 文件。
- 海报专用路径不保存网络失败负缓存；通用元数据目录请求失败也不再永久记为缺失。
- 卡片首次失败后间隔 6 秒重试，最多总计 3 次；服务共享 5 秒退避。离屏取消重试，最终失败显示“暂无缩略图”。
- 115 抽帧优先最低清晰度转码，不再为已有转码额外解析原画。播放器选源规则不变。
- 新增失败退避后恢复 XCTest；Windows 仅完成源码预检，26 个 iOS XCTest 尚未执行。
- WebDAV 无海报且系统不支持的编码/容器仍可能无法抽帧；“VLC 能播放”不代表 AVFoundation 能生成封面。需要同名海报或服务端缩略图，当前没有新增 VLC 抽帧实现。

## 2026-09-09 相册式界面与性能优化

详见 `UI_PERFORMANCE_REVIEW.md`。本轮新增 4 个 XCTest，总计 30 个；本机只执行源码预检，没有执行 iOS XCTest。

- 默认启用紧凑方形网格；资料库右上角“显示 → 相册式方形网格”可切回宽屏封面。收藏沿用同一偏好。
- 轻按回弹、封面淡入、iOS 18+ 系统缩放转场保持可用，并适配“减少动态效果”；移除网格向所有子视图强制清空动画的事务。
- 可见任务优先，预加载逐个进行；图片请求和耗时抽帧分开限流，播放继续抢占两类工作。
- OpenList 的已有刷新请求同时收集最多 10,000 条返回项目中的 thumb 地址；无权限、无缩略图或失败时继续用海报/抽帧。不会新增网盘应用授权。
- 修复筛选空页阻断分页、旧刷新响应覆盖新状态、换源复用旧封面、照片收藏误入视频播放器及取消起播后继续启动的问题。
- `Tests/preflight.py` 检查全部 App Swift 源码。AppState 和 PlayerModel 中原有的解析器诊断与 Git 基线一致，单独标注 BASELINE，不表示已通过 Swift 编译。

## 2026-09-20 资料库恢复、全局补图和拖动交互（2.2.3 / 27）

- 115 已缓存目录不再因 5 分钟 TTL 阻塞首屏；视图重建可先恢复应用内目录快照，再静默刷新。首次目录结果在磁盘缩略图预热之前发布。
- 筛选条放入固定 VStack，使用固定导航栏搜索框，并裁剪滚动内容，避免下拉时进入搜索框区域。
- UICollectionView 开启可取消预取；复用内容使用媒体 ID，只重配发生变化的可见卡片；缓存封面不再淡入。
- 应用处于前台且已解锁时，逐页遍历根目录和子目录，串行补齐缺失图片/抽帧；跳过已有磁盘缓存，不把全库图片解码进内存。进入播放暂停并取消后台目录请求和封面任务，退出后恢复。网络失败退避 5 分钟，扫描结束后 5 分钟再次检查。
- 长按搜索结果可定位回完整目录；网格和列表均支持定位。右上角菜单“重载缩略图”重启缺失项补齐，保留已缓存图片；移除卡片“重试缩略图”。
- 选择圆圈缩小到 18pt；网格选择模式支持按住约 0.25 秒拖选，靠近上下边缘自动滚动；回退拖动恢复手势开始时的选择，照片不纳入视频选择。
- 系统播放器按 loadedTimeRanges 的实际区间绘制缓存条，不将区间空洞视为缓存；切源清空旧区间。拖动最终定位允许 0.1 秒误差，已落到目标附近时避免重复 seek；超过 180ms 的实际 seek 才显示拖动加载提示。
- 新增 5 个 XCTest，合计 52 个；GitHub 工作流增加 iOS Simulator 回归任务，与 IPA Release 构建分别报告。

Windows 源码预检与 git diff --check 通过；这不等于 Xcode 编译或真机验证。最终 Xcode / XCTest 结果以对应提交的 GitHub Actions 为准。

真机重点检查：离开资料库超过 5 分钟后返回、下拉搜索区域、快速来回滚动、搜索定位到后续分页、拖选及边缘滚动、双指缩放、后台补图时起播、拖到实际缓存区间/空洞、AVPlayer/VLC/画中画。网络缓存并不保证任意帧可瞬时解码，实际首帧和拖动耗时仍需同源同网对比。

参考：Apple [预取接口](https://developer.apple.com/documentation/uikit/uicollectionviewdatasourceprefetching)、[不连续缓冲区间](https://developer.apple.com/documentation/avfoundation/avplayeritem/loadedtimeranges)。


## 2026-09-20 拖动预览和构建任务（2.2.4 / 28）

- 按要求移除 GitHub `regression` 作业及其结果上传；推送仅构建完整 VLC IPA。保留已有测试源码，便于本地按需执行。
- AVPlayer 拖动时使用快速邻近关键帧定位，合并手势事件、每次只保留一个执行中的 seek；完成后留出一帧显示时间。松手后才按 0.1 秒容差精确定位，已落点直接恢复，过期请求仅在松手时取消一次。
- 暂停、退出、换源会取消未执行的拖动任务，旧回调不能抢回播放或跳回旧位置。准备中的项目等到 ready 再执行最新拖动目标。
- 拖动期间保留视频画面，移除 180ms 人工加载提示；松手后实际等待持续 350ms 才显示加载圈。
- VLC 将 20 次/秒的触点触发改为最多 8 次/秒的最新目标合并，并补交最后一次移动；移除松手固定转圈和相同目标的重复提交。
- 不新增 AVAssetImageGenerator 抽帧下载、第二个播放器、额外解码器或全片预取。原有播放码率、HDR、音频、网络缓存参数保持不变。

Windows 只能进行源码预检；最终类型检查以完整 Xcode Release 构建为准。这里的快速预览是播放器显示邻近可解码帧，并非保证网络视频逐帧实时预览。已缓冲的时间段也可能需要关键帧解码，跨出缓冲区仍需网络请求。

真机检查：同一视频在已缓冲段慢拖、快速来回拖、连续多次拖动、未缓冲段松手、暂停时拖动、拖动后立即退出/换源；确认画面有更新、最终时间正确、暂停状态不被恢复、加载圈只在实际等待时出现。分别检查系统播放器与 VLC。

依据：[Apple seek 容差与解码延迟](https://developer.apple.com/documentation/avfoundation/avplayer/seek(to:tolerancebefore:toleranceafter:))、[Apple QA1820 合并拖动定位](https://developer.apple.com/library/archive/qa/qa1820/_index.html)。


## 2026-09-24 默认画质新增 1080P（2.2.5 / 29）

- 仅在设置 → 播放 → 默认画质新增“1080P 优先”，保留用户原有默认值。优先选 115 返回的 definition=4 转码；没有时先选较低转码，再回退到其他实际可用源，当前实际画质沿用既有播放器显示。
- 播放菜单、手动切换画质和续播逻辑保持原样；不新增播放菜单入口或不可用提示。
- GitHub 仍只构建 IPA，不增加 regression 任务或测试产物。

验证：Windows 源码预检和 diff 检查；完整 Release 构建结果以对应 GitHub Actions 为准。真实 115 账号的视频转码可用性及长视频播放流畅度仍需真机验证。


## 2026-09-24 全库补图重载与播放优先（2.2.6 / 30）

- 重载按钮唤醒现有扫描队列；扫描中点击不再重建 App task，也不同时取消所有可见卡片任务。空闲时立即唤醒，执行中先完成当前遍历，再检查缺失项。
- 从资料库根目录遍历子目录和全部分页，最多并行处理 3 张缺失封面；网络下载最多 3 路、抽帧最多 2 路，继续服从原有播放优先门控。某个慢视频不会独占串行扫描。
- 取消扫描/卡片的单个等待者会立即返回，不再等待其他卡片共享的网络任务完成，也不会删除其他客户端仍在用的请求。
- 跳过内存和磁盘已有图片；手动重载后的目录遍历重新读取分页，避免旧目录快照漏掉未浏览内容。单页请求 30 秒截止，失败不阻挡其他目录。
- 点播仍先暂停补图、取消目录与图片/抽帧请求，退出后恢复；此次没有改动 1080P 选择或播放器参数。
- 补充 5 个 XCTest 源码用例：慢任务隔离、重复重载合并、空闲唤醒竞争、目录超时、取消共享请求的单个等待者。已有测试覆盖全库分页/子目录/缓存跳过和播放抢占。按用户要求不恢复 CI regression 作业；这些 XCTest 本轮未执行。

Windows 源码预检和完整 IPA Release 构建用于验证语法及编译；“重载后立刻点播”、全库完成情况与实际网络流畅度仍需真机验证，不能用编译成功替代。
