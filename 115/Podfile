platform :ios, '17.0'
use_frameworks!

target 'Gallery115' do
  # Optional. The app builds without this pod; install it only if you want
  # VLC fallback for MKV / unusual original formats.
  pod 'MobileVLCKit', '~> 3.3'
end
